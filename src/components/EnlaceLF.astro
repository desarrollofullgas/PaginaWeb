---
import ThirdCTA from "../components/ui/buttons/ThirdCTA.astro";

const {
  thirdBtn,
  thirdBtnURL,
} = Astro.props;

interface Props {
  thirdBtn?: string;
  thirdBtnURL?: string;
}
---
<section class="flex flex-wrap items-center justify-center gap-2 px-2 py-5">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
       <div class="text-2xl text-neutral-300 font-semibold">¡HAGAMOS NEGOCIO JUNTOS, CONOCE LA LICENCIA FULLGAS!</div>
        <div class="mt-3 items-center justify-center grid w-full gap-3 sm:inline-flex">
            {thirdBtn && 
            <ThirdCTA title={thirdBtn} url={thirdBtnURL} />
            }
          </div>
    </div>
</section>