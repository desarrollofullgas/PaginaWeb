---
import Parallax from "../../components/Parallax.astro";
import Layout from "../../layouts/Layout.astro";

import { jobs } from "../../constants/jobs";
import CardJob from "../../components/CardJob.astro";
---
<Layout title="FullGas - Bolsa de trabajo">
    <section class="mt-3">
        <Parallax bg="/src/img/backgrounds/bg03.jpg">
            <div class=" max-w-7xl m-auto my-16 sm:my-32 flex flex-col gap-4">
                <h2 class="text-slate-200 text-3xl sm:text-5xl">¡Se parte del buen trato,</h2>
                <h1 class="text-red-500 text-6xl sm:text-8xl font-semibold">Únete al equipo FullGas!</h1>
            </div>
        </Parallax>
    </section>
    <section class="my-4 text-slate-300">
        {jobs.length == 0 ?<h2 class="max-w-7xl m-auto font-medium px-2 text-amber-500 text-5xl mb-6">Sin vacantes disponibles 😢</h2>:<h2 class="max-w-7xl m-auto font-medium px-2 text-amber-500 text-5xl mb-6">Vacantes disponibles 🫣</h2>}
        <div class="max-w-7xl m-auto mt-8 text-balance flex flex-col gap-3">
            {jobs.map((job,index)=><CardJob job={job} href={index}/>)}
        </div>
    </section>
</Layout>